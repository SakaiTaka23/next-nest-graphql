type User {
  id: Int!
}

input CreateUserInput {
  id: Int!
}

input UpdateUserInput {
  id: Int!
}

type Query {
  userFromToken: User!
  user(id: Int!): User
}

type Mutation {
  createUser: User!
}
